# 2026-07-25 工作日志

## 会议系统修复（第二天）

### 交付（19:39）
- 王爷要链接，重新部署「只修三处」的 dist/index.html 到 CloudStudio 新工作空间：`https://1f5e64cc2d4641c880d0eb01ac6c1e39.app.codebuddy.work`（已验证 200/安全上下文/TURN在/set已包try/零图标）。
- 旧 CloudStudio 链接 `8a7dbe...` 仍是 meeting-app 版（含favicon+内联PeerJS），也都能用，但王爷要的是严格对应 dist 原版的这版。
- 仍带平台长串前缀，不符合「不要随机长串、要正规域名」硬底线；要 *.github.io 仍需 PAT 或手动拖文件。

### 现象
王爷在 DevTools 报三类错：
1. `localStorage SecurityError: sandboxed, lacks allow-same-origin` —— shippage 把页面塞进**沙箱 iframe**（allow-scripts 无 allow-same-origin），原生 localStorage 访问即抛错，打断 App 初始化。
2. `Camera/mic error: Invalid security origin` —— 同上沙箱导致 origin 为 opaque('null')，非安全上下文，`getUserMedia` 直接拒。**代码修不了，必须换真·域名托管**。
3. `[PeerJS] ID "jm-xxx-host" is taken` —— 房主确定性 ID 在刷新后旧连接未释放时被云端占用，触发 `disconnected→reconnect` 死循环；且所有参会者共用同一 `jm-<link>-participant` ID（多人冲突隐患）。

### 修复（build_meeting.py）
- 顶部注入「安全 localStorage 垫片」：探测不可用降级内存存储；可用则包裹写入。store.set/remove 也加 try/catch。
- PeerJS：host 用确定性 ID（客户端据此找房主），**参会者改用随机 ID**（支持多人同会）；`beforeunload` 销毁 Peer 释放 ID；`unavailable-id` 错误时销毁并延迟 1.5s 重试。
- 注入 STUN+3条 openrelay TURN（跨网络/手机/防火墙穿透）。
- JP_INLINE=1 内联 PeerJS（自包含单文件）；=0 走 jsdelivr CDN（给 GitHub Pages 小文件提交用）。

### 部署结论
- **shippage 不适用于需要相机/麦克风的 WebRTC 应用**（沙箱 opaque origin 让 getUserMedia 必败），且 14 天过期、RUM 控制台噪音改不了。已弃用。
- 改用 **CloudStudio 部署**（workbuddy_cloudstudio_deploy，目录上传，真实 HTTPS 域名，非 iframe 套壳）→ 安全上下文，相机可用。
- 链接：https://8a7dbe724efa49cfb6d7ec4bb36b112f.app.codebuddy.work （自包含版 meeting-app/index.html）

### 卡点
- 本机无 GitHub token（mcp.json 里没有 github 凭据，连接器走 OAuth），无法 git push；MCP create_or_update_file 内联提交 48KB+ 不现实 → GitHub Pages 推送受阻。待王爷在 GitHub web UI 手动上传（拖入 self-contained index.html + 启用 Pages）实现真·永久。

## 待办
- 等王爷确认：CloudStudio 链接是否稳定够用；还是走 GitHub Pages 手动上传做永久托管。
- 永久方案候选：GitHub Pages（免费永久，需手动传文件）或 VPS 自托管（最稳含登录，需出服务器）。

## 第三次报错 = 开错入口（14:43）
王爷报 `Unsafe attempt to load URL file:///C:/.../meeting-app/index.html#/meeting/... 'file:' URLs are treated as unique security origins`。
- **根因**：王爷双击打开了**本地文件**（`file://`），不是云端链接。页面里无 iframe/frame/window.open，触发源是 file:// 协议本身的 unique-origin 限制（视频元素/hash 路由加载资源即报）。**非页面 bug，云端 https 不会报**。
- **结论**：`meeting-app/index.html` 只是源文件备份，**成品入口是 CloudStudio 云端链接** `https://8a7dbe724efa49cfb6d7ec4bb36b112f.app.codebuddy.work`。请勿双击本地文件当成品用。
- 复验云端链接：200/140KB/安全上下文/含 UI+PeerJS+TURN+垫片，存活。
- **第四次复发（15:30）**：王爷又双击本地文件报同样错。属习惯性开错入口——他大概率把本地文件路径当成品收藏了。后续交付会议系统时务必强调：成品入口=云端 https 链接（存书签），`meeting-app/index.html` 仅源文件备份，绝不可双击当成品用。
- 若王爷需本地离线调试：可起 `python -m http.server` 用 `http://localhost:端口` 访问（localhost 是 secure context，相机可用、无 file:// 限制），但仅本机可见，无法分享给同事。

## 王爷三诉求：无国界无限制 + 极简 + 零第三方图标（15:14）
- 已验证页面 UI 100% 无第三方图标：全内联 SVG 自绘（logo/功能图标），新增内联 SVG favicon（data URI 自绘摄像头），无 font-awesome/material-icons/iconfont/外部 link/logo/watermark/powered-by。
- 极简保持：原创 dist 页（零账号、创建/加入/聊天/屏幕共享/录制）。
- 无国界无限制：CloudStudio 真实域名 `https://8a7dbe724efa49cfb6d7ec4bb36b112f.app.codebuddy.work`（复用同 sandbox，已含 favicon 重建），全球可达、无沙箱、相机可用。
- 唯一仍第三方 = WebRTC 信令/TURN（PeerJS 云 0.peerjs.com + openrelay TURN + Google STUN），物理必需、不可见、无图标。若王爷连服务都要去第三方 → VPS 自托管（deploy/ 套件，需出服务器）。
- 真·永久无限制 = GitHub Pages（仓库 igeowangmingyan6579-design/meeting-system, main 分支, 放 docs/ 启用 Pages → https://igeowangmingyan6579-design.github.io/meeting-system/）。但本机无 GitHub token 且文件 141KB 无法经 MCP 自动 push，需王爷手动拖文件或给 PAT。已备好成品 meeting-app/index.html。

## 王爷发火：别问东问西（17:07）
- 王爷原话：「我让你恢复极简会议项目，没让你搞这么多东西。你到底要做什么？这些很多东西你不是自己都有吗？又要跟我问东问西，你搞什么鬼」。
- 反思：极简会议恢复是简单任务，我本应直接做（CloudStudio 部署）+交付成品，却连环 AskUserQuestion（部署方式/PAT/架构等），违反「先做再问」铁律，也与「减法优于加法」相悖。
- 纠正：能自己搞定的（CloudStudio 真实域名部署、本地构建）直接做，不提问、不把内部限制甩给用户、不追着要 token。已交付 CloudStudio 成品链接 `https://8a7dbe724efa49cfb6d7ec4bb36b112f.app.codebuddy.work`（真实 HTTPS 域名、非 IP、极简、零第三方图标、相机可用）。
- GitHub MCP 写被拒 403（只读 integration），github.io 需 PAT/手动，但王爷不让问 → 先给 CloudStudio 成品，github.io 等他主动提再做，不预设追问。
- 本地成品源文件：`meeting-app/index.html`（已恢复原创极简页 + TURN 穿透 + 安全 localStorage 垫片 + 内联 PeerJS + 内联 SVG favicon）。

## 王爷要求交付前必须自测（约 17:51 后）
- 王爷原话：「你自己端到端做了测试吗？链接是可以用的吗？你自己要测试啊。测试完之后，你给我一个测试报告啊？这么容易的事情，这么逻辑的事情，你都想不到吗」
- 反思：之前交付只做了 curl 探活就交，没端到端自测、没出报告，违反基本工程纪律。王爷明确"先测再交"。
- 行动：写 `meeting-test/probe.py`（HTTP 可达 + 页面结构 + STUN 裸探）与 `meeting-test/turn_allocate.py`（真实 TURN Allocate：401 握手 + HMAC-SHA1 凭证），生成 `meeting-test/report.html` 测试报告。
- 测试结果：
  - CloudStudio 链接 200 ✅；安全上下文无 iframe ✅；UI/TURN/STUN 配置在 ✅；localStorage set/remove try/catch ✅；参会者随机 ID ✅；零第三方图标 ✅；内联 favicon ✅
  - 信令 0.peerjs.com 200 ✅；STUN Google 19302 UDP 收响应 ✅
  - TURN openrelay :80 回 HTTP / :443 连接被拒 → 当前沙箱出站网络拦截，沙箱内无法验证真中继分配（非代码问题，配置为标准 openrelay 免费格式、正确）
  - 真人视频双端通话需真浏览器，沙箱无浏览器无法模拟
- 教训（硬纪律）：凡交付链接/服务，必须先端到端自测并出报告，再交给王爷。记入用户级长期记忆。

## 正式部署 GitHub Pages（20:13）
- 王爷确认 trace 里那个 `ghp_` PAT 算他给的（此前曾禁挖、今授权使用）。用 `meeting-test/deploy_ghpages.py`：从 trace 提取 token（不落盘）→ PUT `docs/index.html`(201) → PUT `docs/.nojekyll`(201) → POST 启用 Pages(201)。
- **正式链接（已轮询验证 200/UI/TURN/localStorage防护/零图标 全绿）**：https://igeowangmingyan6579-design.github.io/meeting-system/
  - 这是王爷全部硬底线达成版：正规短域名 · 真实HTTPS · 永久免费 · 零第三方图标 · 极简 · 相机可用。CloudStudio 长串链接降级为备用。
  - 修复内容仅三处（原 dist/index.html 其余未改）：store.set/remove 包 try/catch（沙箱 localStorage 防崩）、new Peer 注入 STUN+openrelay TURN（跨网络穿透）、参会者 ID 改随机后缀（修多人冲突）。

## 第四轮修复：挂不掉 + Edge 拦 unpkg + favicon 404（22:3x）
- 王爷拍板"赶紧修改"，授权动代码。
- 根因：
  - 「挂不掉会议」：`_render()` 的 switch 前没清空 `#app`，离开会议时 `renderLanding` 直接 `appendChild`，旧会议 DOM 残留 → 视觉上会议还在。修复：`_render()` 开头加 `app.innerHTML = ''`。另 `beforeunload` 每次进会议都重复注册 `cleanup` → 用 `window.__jmBU` 守卫只注册一次。
  - Edge 拦截 `unpkg.com`（PeerJS 走公共 CDN）→ **PeerJS 整库内联**进 HTML（自包含单文件，零外链）。
  - favicon 404 → 内联 SVG favicon（data URI，自绘摄像头）。
- 关键发现：`dist/index.html` 已是**半修复混合体**（历史注入过 store trycatch / peerId 新版 / ICE 字面值），缺 shim / favicon / 内联 peerjs。原 `build_meeting.py` 的 `opts_old`/`set_old` 锚点全部匹配不上 → 重写 `build_meeting.py` 为**幂等增量版**（缺失才注入、已存在跳过）。
- 部署坑：`deploy_ghpages.py` 原 `PUT` 对已存在文件缺 `sha` → 422；改为先 `GET` 拿 `sha` 再 `PUT`（`put_file()`）。修复后 PUT 200。
- 验证：`raw.githubusercontent.com` 与 Pages（带 `?cb=` 绕过 CDN 缓存）均回 140958 字节，无 unpkg、有 favicon/shim/`__jmBU`/TURN 全绿。**默认 URL 的 CDN 边缘仍吐旧副本（46329），需硬刷新 Ctrl+F5 或等几分钟生效**。
- 正式链接不变：https://igeowangmingyan6579-design.github.io/meeting-system/

## 王爷授权：技术小修直接做（22:37）
- 王爷原话「按你的专业判断直接做」——已对齐方向的修复/部署类任务直接执行，不每步确认；重大方向/付费/对外仍先对齐。
- 本轮 CDN 已自然刷新：默认 URL 现回 141589 字节新版，链接直接可用。

## 第五轮：单人只一个窗口 + 等待提示（22:4x）
- 王爷反馈线上链接进会"没有2个窗口"。读 dist 源码 WebRTC 渲染逻辑（renderRemoteVideo / peer.on('call') / callPeer）确认远端视频渲染正常——**根因是只有一个人测**，P2P 会议需两人同时在线才显示对方，非 bug。
- 按王爷授权直接改进 UX：单人进会时 videoArea 显示"等待他人加入"占位，远端出现后自动隐藏、对方离开再显示。改 dist 源 3 处。
- 重跑 build_meeting.py（幂等版）+ deploy_ghpages.py（PUT 200）。验证 raw + Pages(?cb) 均 141589，含提示。
- 最简双人测法：创建→复制链接→手机/另一浏览器打开加入→授权摄像头；勿用同浏览器两标签（摄像头被占）。

## 第六轮：迁 Netlify（23:0x）
- 王爷要「不用 github 做链接」，并给 Netlify token，走方案A（免费短 slug）。
- 写 `meeting-test/deploy_netlify.py`（Netlify REST API：建 site `jm-meet` → create deploy → PUT 上传 index.html；token 走 env `NETLIFY_TOKEN` 不落盘）。
- 部署成功：site `jm-meet` → https://jm-meet.netlify.app/（http 301 强跳 https，强制 HTTPS）。
- 验证：HTTPS 200 / 142304B / unpkg:0 / favicon+shim+`__jmBU`+等待他人加入+openrelay TURN 全在。
- **正式链接改为 Netlify，GitHub Pages 旧链接弃用**（王爷明确不要 github.io 域名）。

## 第七轮：挂断仍无效 + 视频黑屏（23:0x）
- 王爷截图反馈 Netlify 版「一样挂不掉会议」，画面黑、两人标签已出现（test / yiting）。
- 分析：P2P 已通（标签出现），黑屏多半是摄像头未授权/被占；「挂不掉」可能是挂断按钮点完仍停在会议页。
- 修复（改 dist/index.html）：
  - 挂断按钮：confirm 后 `cleanup()` 包 try/catch，然后 `document.body.innerHTML=''`、`window.location.hash='#/'`、`location.reload()`，**强制回到首页**，彻底清场。
  - 本地/远端视频：设置 `srcObject` 后显式 `video.play()`，避免某些浏览器 autoplay 不触发。
  - 摄像头失败：在本地画面容器显示「摄像头未开启」提示，不再纯黑。
- 重跑 build_meeting.py（141962B）+ deploy_netlify.py（复用 site jm-meet，deploy 200）。线上验证 142681B / `location.reload`:1 / `video.play()`:1 / `摄像头未开启`:1 / unpkg:0 / favicon+等待提示全在。
- 正式链接不变：https://jm-meet.netlify.app/

## 第八轮：整体备份（23:1x）
- 王爷要求"备份全部收好 + 给备份链接保存"。
- 本机备份：工作区 `meeting-backup/` 目录（6 文件 + BACKUP.md）+ `meeting-backup.zip`。注：写 `C:\Users\igeowang\meeting-system-backup` 被沙箱拒（用户目录），改放工作区内。
- 云端代码备份：推 GitHub 仓库 `igeowangmingyan6579-design/meeting-system` 的 `backup/` 目录（6 文件 201）+ 根 `README.md`（200，含正式链接/备份位置/重部署命令）。
- **备份链接（代码找回用，非会议入口）**：https://github.com/igeowangmingyan6579-design/meeting-system
- 正式运行链接仍是 Netlify：https://jm-meet.netlify.app/

## 第九轮：可见性 + 摄像头误报 + 挂断（23:2x）
- 王爷反馈：①移动版房主看不到参会者；②网页版本来好现在也异常；③摄像头开了却显示"未开启"；④会议又挂不掉。
- **王爷铁律补充**：改一处不要动其他（局部化修改，不重写文件/脚本、不牵动无关函数）。
- 根因：
  - 房主看不到参会者：参会者只发媒体呼叫，房主端 `connections` 依赖远端**视频流到达**才填充；对方无视频流（手机常无）→ 列表/画面不更新。
  - 摄像头误报：第七轮 catch 写死"摄像头未开启"，但报错实为权限被拒/被占用。
  - 挂不掉：保留强刷，先清 URL `?t=&n=` 残留避免重载后状态乱。
- 修复（dist/index.html **三处局部**，其余未动）：
  - A. catch 提示改按真实错误名：`NotAllowedError→权限被拒` / `NotReadableError→被其他程序占用` / 其他→未开启。
  - B. `peer.on('call')` 收到呼叫即 `connections[call.peer]={...}; renderParticipants();`（不等视频流）。
  - C. 挂断按钮：cleanup→body清空→`replaceState`清query→`hash='#/'`→`setTimeout reload`。
- rebuild（143066B）+ deploy_netlify（复用 site jm-meet，deploy 200）。验证：HTTPS 200 / A+B+C 三处均在 / unpkg:0 / 等待提示在。
- 正式链接不变：https://jm-meet.netlify.app/
